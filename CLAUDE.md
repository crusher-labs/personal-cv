@AGENTS.md

## Notes

- JSX-only React 18 SPA; no TypeScript, no test/lint gate, so verify changes by running `npm run dev` / `npm run build` rather than a typecheck.
- Content is still scaffold placeholder; confirm whose CV this becomes before wiring real contact links.

## Compaction

When compacting, preserve: current task, files modified this session, failing checks, decisions made + rationale. Drop exploratory reads. Write durable state to `HANDOFF.md` before compacting.
